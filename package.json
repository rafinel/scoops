{
  "name": "my-turborepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "biome format --write .",
    "skills": "pnpm dlx skills experimental_install",
    "sync:commands": "node scripts/sync-commands.mjs",
    "sync:agents": "node scripts/sync-agents.mjs",
    "sync": "pnpm sync:commands && pnpm sync:agents",
    "test:scripts": "node --test scripts/tests/*.test.mjs",
    "check:test-integrity": "node scripts/check-test-integrity.mjs",
    "check:complexity": "node scripts/check-complexity.mjs",
    "update:complexity-baseline": "node scripts/check-complexity.mjs --update-baseline",
    "test:coverage": "pnpm --filter @scoops/core test:coverage && pnpm --filter server test:coverage && pnpm --filter web test:coverage",
    "check:architecture": "pnpm --filter @scoops/core check:architecture && pnpm --filter @scoops/validation check:architecture && pnpm --filter server check:architecture && pnpm --filter web check:architecture",
    "check:spec-implementation": "node scripts/check-spec-implementation.mjs",
    "check-types": "turbo run check-types",
    "check:code": "pnpm --filter @scoops/core check:code && pnpm --filter @scoops/validation check:code && pnpm --filter server check:code && pnpm --filter web check:code",
    "check:types": "pnpm --filter @scoops/core check:types && pnpm --filter @scoops/validation check:types && pnpm --filter server check:types && pnpm --filter web check:types"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "code-multivitals": "1.6.2",
    "dependency-cruiser": "16.10.4",
    "turbo": "^2.10.8",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
