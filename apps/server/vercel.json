{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm build && corepack pnpm db:migration:apply && if [ \"$SCOOPS_SERVER_APP_MODE\" = \"stg\" ]; then corepack pnpm db:seed; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
